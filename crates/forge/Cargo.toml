[package]
name = "forge"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
anyhow = "1"
common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
