# Kernel modules to include in initrd
# One module per line, # comments and empty lines are ignored.
# These modules are both copied into the initrd and loaded at boot.

# NVMe block device support
crc64
crc64-rocksoft
t10-pi
nvme-core
nvme

# Virtio drivers (dependency order)
virtio_ring
virtio
virtio_blk
virtio_pci_modern_dev
virtio_pci_legacy_dev
virtio_pci
