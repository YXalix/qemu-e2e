[package]
name = "test-example"
version = "0.1.0"
edition = "2021"
description = "示例用例（Rust 入口 + C 测试体经 FFI）：新用例复制本目录改 TESTS 列表与 c/ 内容"

[build-dependencies]
coda-scaffold = { path = "../coda-scaffold" }

[dependencies]
coda = { path = "../coda" }

# 测试组（可选）：声明后二进制装进 /tests/<group>/（init 里名字 = group/case，
# `virtuoso test --only <group>/` 整组选择）；缺省平铺 /tests/。
# [package.metadata.virtuoso]
# group = "smoke"
